/**
 * TextField module barrel export.
 *
 * Re-exports the TextField wrapper component and its props type interface
 * from the TextField implementation module, providing the public API surface
 * for this component directory.
 *
 * Consumed by: src/components/core/index.ts (core category barrel)
 */
export { TextField } from './TextField';
export type { TextFieldProps } from './TextField';
