/**
 * Button module barrel export.
 *
 * Re-exports the Button component and its ButtonProps type interface from the
 * implementation module, providing a clean import surface for consumers and
 * parent barrel aggregators (src/components/core/index.ts).
 *
 * @module components/core/Button
 */
export { Button } from './Button';
export type { ButtonProps } from './Button';
